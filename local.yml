---
- hosts: localhost
  connection: local
  become: true
 
  tasks:
  - name: install packages
    apt:
      name:
        - htop
        - mc
        - tmux
        - net-tools
        - lsof
        - unzip
